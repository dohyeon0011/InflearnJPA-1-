# 스프링 부트에서 따로 만들어 주는 테스트용 테이블(다 주석으로 없어도, 인메모리로 돌아감)
#spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # 기본으로 create-drop으로 해줌
#    properties:
#      hibernate:
#        show_sql: true System.out으로 뿌림
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug  # 하이버네이트가 생성하는(SQL, JPA) 동작들이 로그로 찍혀서 모두 보임.
  org.hibernate.orm.jdbc.bind: trace